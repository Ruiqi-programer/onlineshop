<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.peng.sms.dao.PortalOrderItemDao">

    <insert id="insertList">
        INSERT INTO oms_order_item (
        order_id, order_sn, product_id,
        product_pic, product_name, product_brand,
        product_sn, product_price, product_quantity,
        product_sku_id, product_category_id, product_sku_code,
        promotion_name, promotion_amount, coupon_amount,
        integration_amount, real_amount, product_attr
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.orderId}, #{item.orderSn}, #{item.productId},
            #{item.productPic}, #{item.productName}, #{item.productBrand},
            #{item.productSn}, #{item.productPrice}, #{item.productQuantity},
            #{item.productSkuId}, #{item.productCategoryId}, #{item.productSkuCode},
            #{item.promotionName}, #{item.promotionAmount}, #{item.couponAmount},
            #{item.integrationAmount}, #{item.realAmount}, #{item.productAttr}
            )
        </foreach>
    </insert>

</mapper>
