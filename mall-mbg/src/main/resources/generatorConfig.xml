<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <properties resource="generator.properties"/>
    <context id="MySqlContext" targetRuntime="MyBatis3" defaultModelType="flat">
        <property name="beginningDelimiter" value="`"/>
        <property name="endingDelimiter" value="`"/>
        <property name="javaFileEncoding" value="UTF-8"/>

        <!-- Generate Serializable interface for models -->
        <plugin type="org.mybatis.generator.plugins.SerializablePlugin"/>

        <!-- Generate toString method for Java models -->
        <plugin type="org.mybatis.generator.plugins.ToStringPlugin"/>

        <!-- Overwrite XML mapper files when generating -->
        <plugin type="org.mybatis.generator.plugins.UnmergeableXmlMappersPlugin"/>

        <commentGenerator type="com.peng.sms.CommentGenerator">
            <!-- Remove all auto-generated comments: true = yes, false = no -->
            <property name="suppressAllComments" value="true"/>
            <!-- Suppress adding date in comments -->
            <property name="suppressDate" value="true"/>
            <!-- Add database remarks as comments/annotations -->
            <property name="addRemarkComments" value="true"/>
        </commentGenerator>

        <jdbcConnection driverClass="${jdbc.driverClass}"
                        connectionURL="${jdbc.connectionURL}"
                        userId="${jdbc.userId}"
                        password="${jdbc.password}">
            <!-- Fix issue with MySQL 8.0 driver where code for a specific database is not generated -->
            <property name="nullCatalogMeansCurrent" value="true"/>
        </jdbcConnection>

        <!-- Java model generation configuration -->
        <javaModelGenerator targetPackage="com.peng.sms.model" targetProject="mall-mbg/src/main/java"/>

        <!-- XML mapper generation configuration -->
        <sqlMapGenerator targetPackage="mapper" targetProject="mall-mbg/src/main/resources"/>

        <!-- Java client (mapper interface) generation configuration -->
        <javaClientGenerator type="XMLMAPPER" targetPackage="com.peng.sms.mapper"
                             targetProject="mall-mbg/src/main/java"/>

        <!-- Generate all tables: tableName=% -->
        <table tableName="%">
            <generatedKey column="id" sqlStatement="MySql" identity="true"/>
        </table>
    </context>
</generatorConfiguration>
